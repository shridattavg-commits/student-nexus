spring:
  data:
    mongodb:
      uri: "mongodb+srv://shridattavg_db_user:dattadbms24@studentnexus.iotewlg.mongodb.net/student_nexus_db?retryWrites=true&w=majority&appName=studentnexus"
  security:
    user:
      name: ignored

server:
  address: 0.0.0.0
  # Railway injects PORT env var; falls back to 8080 locally
  port: ${PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:YourSuperSecretKeyForJWTThatIsAtLeast256BitsLong!!}
    expiration-ms: 86400000   # 24 hours
  teacher:
    invite-code: ${TEACHER_INVITE_CODE:NEXUS-TEACHER-2025}
  cloudinary:
    # Set CLOUDINARY_URL in Railway env vars → format: cloudinary://api_key:api_secret@cloud_name
    url: ${CLOUDINARY_URL:cloudinary://placeholder:placeholder@placeholder}
